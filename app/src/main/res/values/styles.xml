<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/actionBar</item>
        <item name="colorPrimaryDark">@color/ntificationsBar</item>
    </style>

    <style name="ClickLista" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimaryDark">@color/ntificationsBar</item>
    </style>

</resources>
